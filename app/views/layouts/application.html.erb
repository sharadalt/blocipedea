<!DOCTYPE html>
<html>
<head>
  <title>Micropedia</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= stylesheet_link_tag    "application", media: "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-inverse">
    <div class="container">
      <div id="navbar-header">
        <ul class="pull-left">
          <p style="color:white;font-family:AIR DARLING;">Micropedia:</p>
          <%= link_to 'Home   ', root_path, style:'color:white;font-weight:bold;' %>
          <%= link_to 'About', welcome_about_path, style:'color:white;font-weight:bold;'%>
        </ul>
        <ul class="pull-right">
          <%= render "devise/shared/loggedin",style:'color:white;font-weight:bold;' %>
          <%= render "welcome/shared/help",style:'color:white;font-weight:bold;'  %>
        </ul>
      </div> 
  </nav> 
  <%#= yield %>

  <%#= debug(flash) %>
  <div class="container">
    <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>">
        <%= value %>
      </div>
    <% end %>
  </div>

  <%= yield :top_content %>

  <div class="container">
    <h1><%= yield :header %></h1>
    <%= yield %>
  </div>
</body>
</html>
