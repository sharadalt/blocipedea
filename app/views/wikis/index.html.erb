
<div class = "row">

<% if current_user.standard? %>
  <h2>Standard User</h2>
<% else %>
  <h2>Premium User</h2>
<% end %>
  
<%= link_to "new wiki", new_wiki_path, class: 'btn btn-success new-wiki' %>
<h4>My Wikis</h4>
<% if current_user.standard? %>
    <%= link_to "Click and Pay to become Premier User", new_charge_path, class: 'btn btn-primary btn-md' %>
<% end %>
<table>
  <tr>
    <th>Wiki(click wiki link below to edit) </th>
    <th>Wiki(click link to show) </th>
    <th>Wiki(click link to delete) </th>
  </tr>
  <% @wikis.each do |wiki| %>
    <tr>
        <%# if policy(wiki).edit? %>
          <td><%= link_to wiki.title, edit_wiki_path(wiki), style:'color:blue' %></td>
        <%# end %>
        <%# if policy(wiki).show? %>
          <td><%= link_to "show", [wiki], method: :get, style:'color:green' %></td>
        <%# end %>
        <%# if policy(wiki).destroy? %>
          <td><%= link_to "delete", [wiki], method: :delete , style:'color:red'%></td>
          
        <%#end %>
      </p>
    </tr>
  <% end %>
</table>

<% if current_user.premium? %>
 
  <p class="ex1">You are premium user! </p>
  If you downgrade your account, your private wikis will become public!!
       You want to do it? 
   
  <%= link_to "Yes", wiki_downgrade_path(current_user), class: 'btn btn-primary' %> 
  <%#= link_to "Yes", wiki_downgrade_path(current_user) %>
      
    
  <%= link_to "No", wikis_path(current_user), class:'btn btn-primary' %>
   
  <%#= link_to "down_grade_account", wiki_downgrade_path(current_user), class: 'btn btn-primary btn-md' %>
<% end %>
</div>

